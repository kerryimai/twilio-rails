##development
After cloning the repo, run:
`bundle install`
`rails db:create`
`rails db:migrate`

To start the server:
`rails s`

Site is hosted on:
https://twilio-kerry.herokuapp.com/

Phone Number to call: 2018579192
